version: "3.8"

services:
#  redis:
#    image: redis:7-alpine
#    container_name: redis_server
#    ports:
#      - "6379:6379"
#    healthcheck:
#      test: [ "CMD", "redis-cli", "ping" ]
#      interval: 5s
#      timeout: 2s
#      retries: 5
#    restart: unless-stopped

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      - postgres
    restart: unless-stopped

  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Szabikah1
      POSTGRES_DB: card_game_db
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d card_game_db" ]
      interval: 5s
      timeout: 2s
      retries: 5
    restart: unless-stopped
#
#  app:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    container_name: card-game
#    ports:
#      - "8080:8080"
#      - "5005:5005"
#    depends_on:
#      - redis
#      - postgres
#    environment:
#      - SPRING_PROFILES_ACTIVE=prod
#      - SPRING_DATA_REDIS_HOST=redis
#      - SPRING_DATA_REDIS_PORT=6379
#      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/card_game_db
#      - SPRING_DATASOURCE_USERNAME=postgres
#      - SPRING_DATASOURCE_PASSWORD=Szabikah1
#
#    restart: on-failure

volumes:
  postgres-data: